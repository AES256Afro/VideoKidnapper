# Contributing

Thanks for considering a contribution. This is a small project and issues / PRs are welcome.

## Getting started

```bash
git clone https://github.com/AES256Afro/VideoKidnapper.git
cd VideoKidnapper
pip install -r requirements.txt
pip install pytest
```

Run the test suite before you change anything so you know what "green" looks like:

```bash
python -m pytest tests/ -q
```

The app boots with:

```bash
python main.py           # GUI
python main.py --help    # CLI flags
```

The full prerequisite installer runs from **⚙ Setup** inside the GUI — use it on a fresh machine to verify everything still auto-installs cleanly.

## Project layout

```
videokidnapper/
├── app.py              # ctk root + header + tabs + shortcuts + update check
├── config.py           # presets, supported platforms, window sizes
├── core/
│   ├── downloader.py       # yt-dlp wrapper, per-platform options
│   ├── ffmpeg_backend.py   # trim / GIF / MP4 / concat / waveform / HW encoder probing
│   ├── preview.py          # LRU-capped frame cache
│   └── screen_capture.py   # mss-based recording
├── ui/
│   ├── trim_tab.py / url_tab.py / history_tab.py / debug_tab.py
│   ├── video_player.py     # canvas preview + text overlay + crop overlay + DnD + play
│   ├── widgets.py          # range slider, toast, platform chip, timestamp entry
│   ├── export_options.py / export_dialog.py / share_panel.py
│   ├── text_layers.py / multi_range.py / batch_queue.py / waveform.py
│   ├── setup_dialog.py     # prereq checklist
│   └── theme.py            # design tokens; dark + light palettes
└── utils/
    ├── settings.py         # JSON-backed preferences with schema migration
    ├── ffmpeg_escape.py    # drawtext + filter-arg escaping
    ├── prereq_check.py     # detect / auto-install FFmpeg + pip packages
    ├── dnd.py              # tkinterdnd2 bootstrap
    ├── share.py / github_update.py / srt_parser.py / size_estimator.py
    ├── file_naming.py / time_format.py / ffmpeg_check.py
```

## Coding conventions

- 4-space indent, no tabs. Snake case for identifiers.
- Prefer the project's **theme tokens** (`T.ACCENT`, `T.BG_SURFACE`, etc.) over hard-coded hex.
- Use `T.font(...)` and `T.button(...)` factories so widgets stay consistent.
- **Escape everything** that flows into an ffmpeg filter argument through `utils/ffmpeg_escape.py`. Never format user text directly into a filter string.
- Add a test if you change filter construction, URL detection, escape logic, settings schema, or any pure helper.
- GUI-only changes don't need tests, but please verify `python main.py` still boots and the affected tab renders.

## Tests

The suite is pure-logic and runs in under a second on a laptop. It intentionally avoids network and subprocess calls — `test_github_update.py` tests parsing only, not the HTTP path.

```bash
python -m pytest tests/ -v
```

If you add a feature that has a meaningful pure surface, add a file under `tests/test_<feature>.py`.

## Regenerating screenshots

The README images are generated by `scripts/capture_screenshots.py`, which drives the app through each state and grabs the window. To regenerate:

```bash
ffmpeg -y -f lavfi -i "testsrc=duration=6:size=1280x720:rate=24" \
       -f lavfi -i "sine=frequency=440:duration=6" \
       -c:v libx264 -c:a aac -pix_fmt yuv420p /tmp/vkshots/demo.mp4

VK_DEMO_VIDEO=/tmp/vkshots/demo.mp4 python scripts/capture_screenshots.py
```

## Recording the README demo GIF

`scripts/record_demo.py` uses the same synthetic demo clip to produce an animated tour (`assets/screenshots/demo.gif`). The script drives the app through load → scrub → queue → text layer → URL tab → history while `mss` captures frames at 10fps, then encodes a palette-optimised GIF with FFmpeg.

```bash
VK_DEMO_VIDEO=/tmp/vkshots/demo.mp4 python scripts/record_demo.py
```

## Cutting a release

Once a PR is merged into `main` and you're ready to tag:

```bash
./scripts/tag_release.sh v1.0.0 "Initial public release"
```

The script verifies you're on `main` with a clean tree, runs tests + ruff one more time, creates an annotated tag, and pushes it. It then prints a reminder to open **Releases → Draft a new release** on GitHub and paste the body from `.github/RELEASE_NOTES_vX.Y.Z.md`.

## Submitting a PR

1. Fork, branch off `main` with a feature-style name (`feat/…`, `fix/…`, `docs/…`).
2. Keep commits focused. Use a descriptive subject line.
3. Run `python -m pytest tests/ -q` and confirm all green.
4. Open the PR against `main` with a short **Summary** and a **Test plan** checklist.

## Reporting bugs

Open a GitHub issue with:
- OS + Python version
- Steps to reproduce
- The full traceback or ffmpeg error from the **Debug** tab (the Debug tab captures stderr output including ffmpeg failures)
- If the issue involves a URL, whether it happens with public and private content

## License policy

VideoKidnapper is licensed under **Apache License 2.0** and that is the *only* license accepted for contributions.

By submitting a pull request you agree that your contribution is licensed under Apache-2.0 under the [in-bound = out-bound](https://opensource.guide/legal/#does-my-project-need-an-additional-contributor-agreement) convention (Apache-2.0 §5). No CLA required — submitting the PR is the agreement.

**Every `.py` file must start with the SPDX header.** Two lines at the top of the file (below any shebang):

```python
# SPDX-FileCopyrightText: 2026 Your Name <contact>
# SPDX-License-Identifier: Apache-2.0
```

The test in [`tests/test_license_headers.py`](tests/test_license_headers.py) enforces this — CI fails on any missing header. If you add new source files, paste the two lines and you're done.

**Do not re-license individual files** (GPL, MIT, BSD, etc.). If you need to integrate code from a differently-licensed project, open an issue first so we can discuss compatibility before the PR.

## Security

See [SECURITY.md](SECURITY.md) for how to report vulnerabilities.
