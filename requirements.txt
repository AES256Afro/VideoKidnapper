# Python 3.9+ (tested on 3.11–3.14). customtkinter 5.2 works with all three.
customtkinter>=5.2.0,<6.0
Pillow>=10.0.0,<13.0
mss>=9.0.0,<11.0
yt-dlp>=2024.0.0

# Optional — enables drag-and-drop on the preview canvas. App degrades
# gracefully if this is missing.
tkinterdnd2>=0.3.0
