# Python 3.9+ (tested on 3.11–3.14). customtkinter 5.2 works with all three.
customtkinter>=5.2.0,<6.0
Pillow>=10.0.0,<12.0
mss>=9.0.0,<11.0
yt-dlp>=2024.0.0

# Optional — enables drag-and-drop on the preview canvas. App degrades
# gracefully if this is missing.
tkinterdnd2>=0.3.0

# Optional — enables real-time in-app playback with audio (no more
# "Play in System Player" workaround). App falls back to the original
# 8-fps frame-scrub preview when any of these is missing.
imageio-ffmpeg>=0.5.0
sounddevice>=0.4.6
numpy>=1.24
